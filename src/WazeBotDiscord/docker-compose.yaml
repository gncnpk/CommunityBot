version: "3.8"
services:
    communitybot3.1:
        build: 
            context: .
            dockerfile: Dockerfile.dotnet3.1
        image: communitybot:dotnet-3.1-alpine
        env_file: communitybot.env
        restart: always
